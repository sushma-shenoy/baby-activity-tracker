rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The app currently stores activity data locally. Keep Firestore closed
    // until a server-backed data model and per-user authorization are added.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
